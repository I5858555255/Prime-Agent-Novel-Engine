[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rlm-skill-prime_envs"
version = "0.1.0"
description = "RLM skill for Prime Intellect environment CLI operations."
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["rlm"]

[project.scripts]
prime_envs = "rlm.skill:cli"

[tool.setuptools.packages.find]
where = ["src"]
